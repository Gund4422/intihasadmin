# Intiha's Admin
Over a Thousand times faster than IY on a i3!
Why? IA is literally O(1), not O(n), since IY is $O(n)$. On a modern PC, a loop of 500 strings takes microseconds. On my i3, which is likely already struggling with 8GB of RAM and background Windows processes, that loop is fighting for CPU cycles.
